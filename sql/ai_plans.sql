CREATE TABLE IF NOT EXISTS ai_plans (id INT AUTO_INCREMENT PRIMARY KEY, user_id INT NULL, destination VARCHAR(100) NOT NULL, duration INT NOT NULL, vibe VARCHAR(50) NOT NULL, budget VARCHAR(50) NOT NULL, itinerary_data LONGTEXT NOT NULL, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE);
